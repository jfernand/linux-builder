[package]
name = "buildpacks"
version = "0.1.0"
edition = "2024"

[dependencies]
buildpack-core = { path = "../buildpack-core" }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.151"
toml = "1.1.6"
